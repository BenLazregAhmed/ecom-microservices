eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  cloud:
    config:
      override-system-properties: false
management:
  zipkin:
    tracing:
      endpoint: http://192.168.134.3:9411/api/v2/spans
      sampling:
        probability: 1.0